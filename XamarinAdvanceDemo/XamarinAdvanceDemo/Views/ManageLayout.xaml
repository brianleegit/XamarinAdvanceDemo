<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinAdvanceDemo.Views.ManageLayout"
             Title="Manage Person Group">
  <ContentPage.Content>
    <StackLayout
      BackgroundColor="{StaticResource BrandColor}">
      <ListView x:Name ="peoplelist"
					HasUnevenRows="true"
					SeparatorVisibility="None" >
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <StackLayout  Margin="10">
                <Label Text="{Binding Name}" Font="{StaticResource TitleFont}" TextColor="{StaticResource PrimaryTextColorOnDark}" Margin="5" />
                <Label Text="{Binding PicNum}" Font="{StaticResource SubheadFont}" TextColor="{StaticResource SecondaryTextColorOnDark}" Margin="5"/>
              </StackLayout>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      
      <Button Text="Add People"
            TextColor="{StaticResource PrimaryTextColor}"
            Font="{StaticResource ButtonFont}"
            BackgroundColor="{StaticResource BrandAccentColor}"
            HorizontalOptions="Center"
            VerticalOptions="CenterAndExpand"
            WidthRequest = "250"
            HeightRequest = "80"
            x:Name="AddNew"
        />
    </StackLayout>
  </ContentPage.Content>
  
</ContentPage>